spring:
  application:
    name: backend
  datasource:
    url: jdbc:mariadb://localhost:3306/crypto_db_test?allowPublicKeyRetrieval=true&useSSL=false
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: admin
  flyway:
    url: jdbc:mariadb://localhost:3306/crypto_db_test?allowPublicKeyRetrieval=true&useSSL=false
    schemas: crypto_db_test
    user: root
    password: admin